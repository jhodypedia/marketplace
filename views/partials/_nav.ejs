<nav class="navbar navbar-expand-lg bg-body-tertiary border-bottom">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">RBLX.Store</a>
    <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#nav"><span class="navbar-toggler-icon"></span></button>
    <div class="collapse navbar-collapse" id="nav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item"><a class="nav-link" href="/items">Katalog</a></li>
        <% if (user?.role === 'admin') { %>
          <li class="nav-item"><a class="nav-link" href="/admin">Admin</a></li>
        <% } %>
      </ul>
      <ul class="navbar-nav">
        <li class="nav-item"><a class="nav-link" href="/cart">Keranjang</a></li>
        <% if (!user) { %>
          <li class="nav-item"><a class="nav-link" href="/login">Masuk</a></li>
          <li class="nav-item"><a class="nav-link" href="/register">Daftar</a></li>
        <% } else { %>
          <li class="nav-item"><span class="nav-link">Hi, <%= user.name %></span></li>
          <li class="nav-item">
            <form method="post" action="/logout">
              <button class="btn btn-link nav-link">Keluar</button>
            </form>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
